export declare class Credential {
    private _networkID;
    private _node;
    private _address;
    private _collection;
    constructor(networkID: string, provider?: string);
    private _setNode;
    private _setChain;
}
