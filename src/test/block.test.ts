import { Block } from "../block";

describe("Currency", () => {
  let block: Block;

  beforeEach(() => {
    block = new Block();
  });

  test("block.getAll()", () => {
    // block.getAll() test
  });

  test("block.get()", () => {
    // block.get() test
  });

  test("block.operations()", () => {
    // block.operations() test
  });
});
