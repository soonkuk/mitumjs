import {
  currency,
  nodeKey,
  genesis,
  account1,
  account2,
  account3,
  accountEther1,
  accountEther2,
  accountEther3,
  BTC,
  ETH,
  multi1,
  multi2,
} from "./dummy";

import { Block } from "../block";

describe("Currency", () => {
  let block: Block;

  beforeEach(() => {
    block = new Block();
  });

  test("block.getAll()", () => {
    // block.getAll() test
  });

  test("block.get()", () => {
    // block.get() test
  });

  test("block.operations()", () => {
    // block.operations() test
  });
});
