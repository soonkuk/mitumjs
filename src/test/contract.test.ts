import { Contract } from "../contract";

describe("Currency", () => {
  let contract: Contract;

  beforeEach(() => {
    contract = new Contract();
  });

  describe("nft", () => {
    test("nft.trasfer()", () => {
      // nft.trasfer() test
    });
  });
});
